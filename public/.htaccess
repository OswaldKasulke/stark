RewriteEngine On

# Use one canonical origin for every request:
# https://immobilienmakler-bergisch-gladbach.de
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} !^immobilienmakler-bergisch-gladbach\.de$ [NC]
RewriteRule ^ https://immobilienmakler-bergisch-gladbach.de%{REQUEST_URI} [R=301,L,NE]

# Send the character set in the HTTP header, not just in the markup.
AddDefaultCharset UTF-8
